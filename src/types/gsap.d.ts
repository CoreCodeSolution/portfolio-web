// Ambient module declarations for GSAP plugins
// gsap ships its own types; this file holds project-level augmentations.

declare module 'gsap/dist/ScrollTrigger' {
  export { ScrollTrigger } from 'gsap'
}
