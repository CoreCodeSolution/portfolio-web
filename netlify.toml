[build]
  command = "pnpm build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for client-side (vue-router history mode) routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
